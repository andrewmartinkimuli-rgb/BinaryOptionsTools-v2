{
  "new": {
    "default": "## `new()`\n\nCreates a default validator that accepts **all** incoming messages.\n\nUse this when you want a `RawHandler` that receives every WebSocket message without any filtering.\n",
    "python": "### Python\n\n```python\nfrom binaryoptionstoolsuni import Validator\n\nv = Validator.new()\nassert v.check('any message') == True\nassert v.check('') == True\n\n# Use with raw handler to receive everything\nhandler = await api.create_raw_handler(Validator.new(), None)\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.new()\nassert(v.check(\"any message\"))\nassert(v.check(\"\"))\n\n// Use with raw handler\nval handler = api.createRawHandler(Validator.new(), null)\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.new()\nassert(v.check(message: \"any message\"))\nassert(v.check(message: \"\"))\n\n// Use with raw handler\nlet handler = try await api.createRawHandler(validator: Validator.new(), keepAlive: nil)\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.New();\nDebug.Assert(v.Check(\"any message\"));\nDebug.Assert(v.Check(\"\"));\n\n// Use with raw handler\nvar handler = await api.CreateRawHandler(Validator.New(), null);\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorNew()\nif !v.Check(\"any message\") { panic(\"expected true\") }\n\n// Use with raw handler\nhandler, err := api.CreateRawHandler(bot.ValidatorNew(), nil)\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.new\nraise unless v.check('any message')\nraise unless v.check('')\n\nhandler = api.create_raw_handler(BinaryOptionsToolsUni::Validator.new, nil)\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.new();\nconsole.assert(v.check('any message') === true);\nconsole.assert(v.check('') === true);\n\nconst handler = await api.createRawHandler(Validator.new(), null);\n```\n"
  },
  "regex": {
    "default": "## `regex(pattern)`\n\nCreates a validator that accepts messages matching a regular expression.\n\n### Arguments\n\n- `pattern` — A regular expression string. Must be a valid regex or a `UniError::Validator` is returned.\n\n### Errors\n\nReturns `UniError::Validator` if `pattern` is not a valid regular expression.\n\n### Notes\n\nThis is the only `Validator` constructor that can fail. All others are infallible.\n",
    "python": "### Python\n\n```python\nfrom binaryoptionstoolsuni import Validator\n\n# Match Socket.IO frames (start with digits then '[')\nv = Validator.regex(r'^\\d+\\[')\nassert v.check('42[\"ping\"]') == True\nassert v.check('hello') == False\n\n# Invalid pattern raises\ntry:\n    Validator.regex('(unclosed')\nexcept Exception as e:\n    print(f'Bad regex: {e}')\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.regex(\"\"\"^\\d+\\[\"\"\")\nassert(v.check(\"42[\\\"ping\\\"]\"))\nassert(!v.check(\"hello\"))\n\ntry {\n    Validator.regex(\"(unclosed\")\n} catch (e: UniException.Validator) {\n    println(\"Bad regex: ${e.message}\")\n}\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = try Validator.regex(pattern: #\"^\\d+\\[\"#)\nassert(v.check(message: \"42[\\\"ping\\\"]\"))\nassert(!v.check(message: \"hello\"))\n\ndo {\n    _ = try Validator.regex(pattern: \"(unclosed\")\n} catch let err as UniError {\n    if case .validator(let msg) = err { print(\"Bad regex: \\(msg)\") }\n}\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.Regex(@\"^\\d+\\[\");\nDebug.Assert(v.Check(\"42[\\\"ping\\\"]\"));\nDebug.Assert(!v.Check(\"hello\"));\n\ntry {\n    Validator.Regex(\"(unclosed\");\n} catch (UniException.Validator ex) {\n    Console.WriteLine($\"Bad regex: {ex.Message}\");\n}\n```\n",
    "go": "### Go\n\n```go\nv, err := bot.ValidatorRegex(`^\\d+\\[`)\nif err != nil { panic(err) }\nif !v.Check(`42[\"ping\"]`) { panic(\"expected true\") }\nif v.Check(\"hello\") { panic(\"expected false\") }\n\n_, err = bot.ValidatorRegex(\"(unclosed\")\nif err != nil { fmt.Printf(\"Bad regex: %v\\n\", err) }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.regex('^\\d+\\[')\nraise unless v.check('42[\"ping\"]')\nraise if v.check('hello')\n\nbegin\n  BinaryOptionsToolsUni::Validator.regex('(unclosed')\nrescue => e\n  puts \"Bad regex: #{e.message}\"\nend\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.regex('^\\\\d+\\\\[');\nconsole.assert(v.check('42[\"ping\"]') === true);\nconsole.assert(v.check('hello') === false);\n\ntry {\n    Validator.regex('(unclosed');\n} catch (e) {\n    console.error(`Bad regex: ${e}`);\n}\n```\n"
  },
  "starts_with": {
    "default": "## `starts_with(prefix)`\n\nCreates a validator that accepts messages whose text begins with the given prefix string.\n\n### Arguments\n\n- `prefix` — The string the message must start with\n\nThis constructor is infallible.\n",
    "python": "### Python\n\n```python\nv = Validator.starts_with('42')\nassert v.check('42[\"ping\"]') == True\nassert v.check('hello') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.startsWith(\"42\")\nassert(v.check(\"42[\\\"ping\\\"]\"))\nassert(!v.check(\"hello\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.startsWith(prefix: \"42\")\nassert(v.check(message: \"42[\\\"ping\\\"]\"))\nassert(!v.check(message: \"hello\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.StartsWith(\"42\");\nDebug.Assert(v.Check(\"42[\\\"ping\\\"]\"));\nDebug.Assert(!v.Check(\"hello\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorStartsWith(\"42\")\nif !v.Check(`42[\"ping\"]`) { panic(\"expected true\") }\nif v.Check(\"hello\") { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.starts_with('42')\nraise unless v.check('42[\"ping\"]')\nraise if v.check('hello')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.startsWith('42');\nconsole.assert(v.check('42[\"ping\"]') === true);\nconsole.assert(v.check('hello') === false);\n```\n"
  },
  "ends_with": {
    "default": "## `ends_with(suffix)`\n\nCreates a validator that accepts messages whose text ends with the given suffix string.\n\n### Arguments\n\n- `suffix` — The string the message must end with\n\nThis constructor is infallible.\n",
    "python": "### Python\n\n```python\nv = Validator.ends_with('}')\nassert v.check('{\"key\": \"value\"}') == True\nassert v.check('42[\"ping\"]') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.endsWith(\"}\")\nassert(v.check(\"{\\\"key\\\": \\\"value\\\"}\"))\nassert(!v.check(\"42[\\\"ping\\\"]\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.endsWith(suffix: \"}\")\nassert(v.check(message: \"{\\\"key\\\": \\\"value\\\"}\"))\nassert(!v.check(message: \"42[\\\"ping\\\"]\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.EndsWith(\"}\");\nDebug.Assert(v.Check(\"{\\\"key\\\": \\\"value\\\"}\"));\nDebug.Assert(!v.Check(\"42[\\\"ping\\\"]\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorEndsWith(\"}\")\nif !v.Check(`{\"key\": \"value\"}`) { panic(\"expected true\") }\nif v.Check(`42[\"ping\"]`) { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.ends_with('}')\nraise unless v.check('{\"key\": \"value\"}')\nraise if v.check('42[\"ping\"]')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.endsWith('}');\nconsole.assert(v.check('{\"key\": \"value\"}') === true);\nconsole.assert(v.check('42[\"ping\"]') === false);\n```\n"
  },
  "contains": {
    "default": "## `contains(substring)`\n\nCreates a validator that accepts messages containing the given substring anywhere in the text.\n\n### Arguments\n\n- `substring` — The string that must appear somewhere in the message\n\nThis constructor is infallible.\n",
    "python": "### Python\n\n```python\nv = Validator.contains('\"balance\"')\nassert v.check('42[\"balance\",{\"amount\":100}]') == True\nassert v.check('42[\"ping\"]') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.contains(\"\\\"balance\\\"\")\nassert(v.check(\"42[\\\"balance\\\",{\\\"amount\\\":100}]\"))\nassert(!v.check(\"42[\\\"ping\\\"]\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.contains(substring: \"\\\"balance\\\"\")\nassert(v.check(message: \"42[\\\"balance\\\",{\\\"amount\\\":100}]\"))\nassert(!v.check(message: \"42[\\\"ping\\\"]\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.Contains(\"\\\"balance\\\"\");\nDebug.Assert(v.Check(\"42[\\\"balance\\\",{\\\"amount\\\":100}]\"));\nDebug.Assert(!v.Check(\"42[\\\"ping\\\"]\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorContains(`\"balance\"`)\nif !v.Check(`42[\"balance\",{\"amount\":100}]`) { panic(\"expected true\") }\nif v.Check(`42[\"ping\"]`) { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.contains('\"balance\"')\nraise unless v.check('42[\"balance\",{\"amount\":100}]')\nraise if v.check('42[\"ping\"]')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.contains('\"balance\"');\nconsole.assert(v.check('42[\"balance\",{\"amount\":100}]') === true);\nconsole.assert(v.check('42[\"ping\"]') === false);\n```\n"
  },
  "ne": {
    "default": "## `ne(validator)`\n\nCreates a validator that **negates** another validator — accepts messages that the inner validator would reject, and rejects messages it would accept.\n\n### Arguments\n\n- `validator` — The validator whose result to invert\n\nThis constructor is infallible.\n",
    "python": "### Python\n\n```python\n# Accept anything that does NOT contain 'error'\nv = Validator.ne(Validator.contains('error'))\nassert v.check('success') == True\nassert v.check('error occurred') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.ne(Validator.contains(\"error\"))\nassert(v.check(\"success\"))\nassert(!v.check(\"error occurred\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.ne(validator: Validator.contains(substring: \"error\"))\nassert(v.check(message: \"success\"))\nassert(!v.check(message: \"error occurred\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.Ne(Validator.Contains(\"error\"));\nDebug.Assert(v.Check(\"success\"));\nDebug.Assert(!v.Check(\"error occurred\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorNe(bot.ValidatorContains(\"error\"))\nif !v.Check(\"success\") { panic(\"expected true\") }\nif v.Check(\"error occurred\") { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.ne(BinaryOptionsToolsUni::Validator.contains('error'))\nraise unless v.check('success')\nraise if v.check('error occurred')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.ne(Validator.contains('error'));\nconsole.assert(v.check('success') === true);\nconsole.assert(v.check('error occurred') === false);\n```\n"
  },
  "all": {
    "default": "## `all(validators)`\n\nCreates a validator that accepts a message only when **every** validator in the list matches (logical AND).\n\n### Arguments\n\n- `validators` — A list of validators that must all match\n\nThis constructor is infallible. An empty list matches all messages.\n",
    "python": "### Python\n\n```python\n# Must start with '42' AND contain 'balance'\nv = Validator.all([\n    Validator.starts_with('42'),\n    Validator.contains('balance'),\n])\nassert v.check('42[\"balance\",{}]') == True\nassert v.check('42[\"deals\",{}]') == False\nassert v.check('hello balance') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.all(listOf(\n    Validator.startsWith(\"42\"),\n    Validator.contains(\"balance\"),\n))\nassert(v.check(\"42[\\\"balance\\\",{}]\"))\nassert(!v.check(\"42[\\\"deals\\\",{}]\"))\nassert(!v.check(\"hello balance\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.all(validators: [\n    Validator.startsWith(prefix: \"42\"),\n    Validator.contains(substring: \"balance\"),\n])\nassert(v.check(message: \"42[\\\"balance\\\",{}]\"))\nassert(!v.check(message: \"42[\\\"deals\\\",{}]\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.All(new List<Validator> {\n    Validator.StartsWith(\"42\"),\n    Validator.Contains(\"balance\"),\n});\nDebug.Assert(v.Check(\"42[\\\"balance\\\",{}]\"));\nDebug.Assert(!v.Check(\"42[\\\"deals\\\",{}]\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorAll([]bot.Validator{\n    bot.ValidatorStartsWith(\"42\"),\n    bot.ValidatorContains(\"balance\"),\n})\nif !v.Check(`42[\"balance\",{}]`) { panic(\"expected true\") }\nif v.Check(`42[\"deals\",{}]`) { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.all([\n  BinaryOptionsToolsUni::Validator.starts_with('42'),\n  BinaryOptionsToolsUni::Validator.contains('balance'),\n])\nraise unless v.check('42[\"balance\",{}]')\nraise if v.check('42[\"deals\",{}]')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.all([\n    Validator.startsWith('42'),\n    Validator.contains('balance'),\n]);\nconsole.assert(v.check('42[\"balance\",{}]') === true);\nconsole.assert(v.check('42[\"deals\",{}]') === false);\n```\n"
  },
  "any": {
    "default": "## `any(validators)`\n\nCreates a validator that accepts a message when **at least one** validator in the list matches (logical OR).\n\n### Arguments\n\n- `validators` — A list of validators where one or more must match\n\nThis constructor is infallible. An empty list rejects all messages.\n",
    "python": "### Python\n\n```python\n# Must contain 'balance' OR 'deals'\nv = Validator.any([\n    Validator.contains('balance'),\n    Validator.contains('deals'),\n])\nassert v.check('42[\"balance\",{}]') == True\nassert v.check('open deals list') == True\nassert v.check('42[\"ping\"]') == False\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.any(listOf(\n    Validator.contains(\"balance\"),\n    Validator.contains(\"deals\"),\n))\nassert(v.check(\"42[\\\"balance\\\",{}]\"))\nassert(v.check(\"open deals list\"))\nassert(!v.check(\"42[\\\"ping\\\"]\"))\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.any(validators: [\n    Validator.contains(substring: \"balance\"),\n    Validator.contains(substring: \"deals\"),\n])\nassert(v.check(message: \"42[\\\"balance\\\",{}]\"))\nassert(v.check(message: \"open deals list\"))\nassert(!v.check(message: \"42[\\\"ping\\\"]\"))\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.Any(new List<Validator> {\n    Validator.Contains(\"balance\"),\n    Validator.Contains(\"deals\"),\n});\nDebug.Assert(v.Check(\"42[\\\"balance\\\",{}]\"));\nDebug.Assert(v.Check(\"open deals list\"));\nDebug.Assert(!v.Check(\"42[\\\"ping\\\"]\"));\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorAny([]bot.Validator{\n    bot.ValidatorContains(\"balance\"),\n    bot.ValidatorContains(\"deals\"),\n})\nif !v.Check(`42[\"balance\",{}]`) { panic(\"expected true\") }\nif !v.Check(\"open deals list\") { panic(\"expected true\") }\nif v.Check(`42[\"ping\"]`) { panic(\"expected false\") }\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.any([\n  BinaryOptionsToolsUni::Validator.contains('balance'),\n  BinaryOptionsToolsUni::Validator.contains('deals'),\n])\nraise unless v.check('42[\"balance\",{}]')\nraise unless v.check('open deals list')\nraise if v.check('42[\"ping\"]')\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.any([\n    Validator.contains('balance'),\n    Validator.contains('deals'),\n]);\nconsole.assert(v.check('42[\"balance\",{}]') === true);\nconsole.assert(v.check('open deals list') === true);\nconsole.assert(v.check('42[\"ping\"]') === false);\n```\n"
  },
  "check": {
    "default": "## `check(message)`\n\nTests whether the given message satisfies this validator's conditions.\n\n### Arguments\n\n- `message` — The string to test\n\n### Returns\n\n`true` if the message matches, `false` otherwise.\n",
    "python": "### Python\n\n```python\nv = Validator.contains('balance')\nprint(v.check('42[\"balance\",{}]'))  # True\nprint(v.check('42[\"ping\"]'))        # False\n\n# Useful for manual filtering in a loop\nhandler = await api.create_raw_handler(Validator.new(), None)\nwhile True:\n    msg = await handler.wait_next()\n    if Validator.contains('balance').check(msg):\n        print(f'Balance update: {msg}')\n```\n",
    "kotlin": "### Kotlin\n\n```kotlin\nval v = Validator.contains(\"balance\")\nprintln(v.check(\"42[\\\"balance\\\",{}]\"))  // true\nprintln(v.check(\"42[\\\"ping\\\"]\"))         // false\n```\n",
    "swift": "### Swift\n\n```swift\nlet v = Validator.contains(substring: \"balance\")\nprint(v.check(message: \"42[\\\"balance\\\",{}]\"))  // true\nprint(v.check(message: \"42[\\\"ping\\\"]\"))         // false\n```\n",
    "csharp": "### C#\n\n```csharp\nvar v = Validator.Contains(\"balance\");\nConsole.WriteLine(v.Check(\"42[\\\"balance\\\",{}]\"));  // True\nConsole.WriteLine(v.Check(\"42[\\\"ping\\\"]\"));         // False\n```\n",
    "go": "### Go\n\n```go\nv := bot.ValidatorContains(\"balance\")\nfmt.Println(v.Check(`42[\"balance\",{}]`))  // true\nfmt.Println(v.Check(`42[\"ping\"]`))         // false\n```\n",
    "ruby": "### Ruby\n\n```ruby\nv = BinaryOptionsToolsUni::Validator.contains('balance')\nputs v.check('42[\"balance\",{}]')  # true\nputs v.check('42[\"ping\"]')         # false\n```\n",
    "javascript": "### JavaScript\n\n```javascript\nconst v = Validator.contains('balance');\nconsole.log(v.check('42[\"balance\",{}]'));  // true\nconsole.log(v.check('42[\"ping\"]'));         // false\n```\n"
  },
  "Validator": {
    "default": "# Validator\n\nFilters incoming WebSocket messages based on configurable matching rules.\n\nA `Validator` is passed to `create_raw_handler()` to control which messages the handler receives. Validators can be composed using `all()`, `any()`, and `ne()` for complex filtering logic.\n\n## Constructors\n\n| Method | Description |\n|--------|-------------|\n| `new()` | Accept all messages (no filter) |\n| `regex(pattern)` | Match by regular expression |\n| `starts_with(prefix)` | Match messages beginning with a string |\n| `ends_with(suffix)` | Match messages ending with a string |\n| `contains(substring)` | Match messages containing a string |\n| `ne(validator)` | Negate another validator |\n| `all(validators)` | All validators must match (AND) |\n| `any(validators)` | At least one validator must match (OR) |\n\n## Instance Methods\n\n- `check(message)` — Returns `true` if the message satisfies this validator's conditions\n\n## Composition Example\n\nValidators are composable. You can build complex filters by nesting them:\n\n```\nall([\n    starts_with(\"42\"),\n    any([\n        contains(\"balance\"),\n        contains(\"deals\")\n    ])\n])\n```\n\nThis matches any message that starts with `\"42\"` AND contains either `\"balance\"` or `\"deals\"`.\n",
    "python": "## Python\n\n```python\nfrom binaryoptionstoolsuni import Validator\n\n# Accept everything\nv = Validator.new()\nassert v.check(\"any message\") == True\n\n# Regex match\nv = Validator.regex(r'^\\d+\\[\"')\nassert v.check('42[\"getBalance\"]') == True\nassert v.check('hello') == False\n\n# Prefix / suffix / substring\nv = Validator.starts_with('42')\nv = Validator.ends_with('}')\nv = Validator.contains('\"balance\"')\n\n# Negate\nv = Validator.ne(Validator.contains('error'))\nassert v.check('success') == True\nassert v.check('error occurred') == False\n\n# AND composition\nv = Validator.all([\n    Validator.starts_with('42'),\n    Validator.contains('balance')\n])\nassert v.check('42[\"balance\",{}]') == True\nassert v.check('42[\"deals\",{}]') == False\n\n# OR composition\nv = Validator.any([\n    Validator.contains('balance'),\n    Validator.contains('deals')\n])\nassert v.check('server balance update') == True\nassert v.check('open deals list') == True\nassert v.check('ping') == False\n\n# Use with raw handler\nhandler = await api.create_raw_handler(\n    Validator.contains('\"balance\"'),\n    None\n)\nresponse = await handler.send_and_wait('42[\"getBalance\"]')\nprint(response)\n```\n",
    "kotlin": "## Kotlin\n\n```kotlin\nimport uniffi.binaryoptionstoolsuni.Validator\n\n// Accept everything\nval open = Validator.new()\nassert(open.check(\"any message\"))\n\n// Regex match\nval v = Validator.regex(\"\"\"^\\d+\\[\"\"\")\nassert(v.check(\"42[\\\"getBalance\\\"]\"))\n\n// Prefix / suffix / substring\nval sw = Validator.startsWith(\"42\")\nval ew = Validator.endsWith(\"}\")\nval ct = Validator.contains(\"\\\"balance\\\"\")\n\n// Negate\nval notError = Validator.ne(Validator.contains(\"error\"))\nassert(notError.check(\"success\"))\nassert(!notError.check(\"error occurred\"))\n\n// AND composition\nval andV = Validator.all(listOf(\n    Validator.startsWith(\"42\"),\n    Validator.contains(\"balance\")\n))\nassert(andV.check(\"42[\\\"balance\\\",{}]\"))\n\n// OR composition\nval orV = Validator.any(listOf(\n    Validator.contains(\"balance\"),\n    Validator.contains(\"deals\")\n))\nassert(orV.check(\"server balance update\"))\nassert(!orV.check(\"ping\"))\n\n// Use with raw handler\nval handler = api.createRawHandler(Validator.contains(\"\\\"balance\\\"\"), null)\nval response = handler.sendAndWait(\"42[\\\"getBalance\\\"]\")\nprintln(response)\n```\n",
    "swift": "## Swift\n\n```swift\nimport binaryoptionstoolsuni\n\n// Accept everything\nlet open = Validator.new()\nassert(open.check(message: \"any message\"))\n\n// Regex match\nlet v = try Validator.regex(pattern: #\"^\\d+\\[\"#)\nassert(v.check(message: \"42[\\\"getBalance\\\"]\"))\n\n// Prefix / suffix / substring\nlet sw = Validator.startsWith(prefix: \"42\")\nlet ew = Validator.endsWith(suffix: \"}\")\nlet ct = Validator.contains(substring: \"\\\"balance\\\"\")\n\n// Negate\nlet notError = Validator.ne(validator: Validator.contains(substring: \"error\"))\nassert(notError.check(message: \"success\"))\nassert(!notError.check(message: \"error occurred\"))\n\n// AND composition\nlet andV = Validator.all(validators: [\n    Validator.startsWith(prefix: \"42\"),\n    Validator.contains(substring: \"balance\")\n])\nassert(andV.check(message: \"42[\\\"balance\\\",{}]\"))\n\n// OR composition\nlet orV = Validator.any(validators: [\n    Validator.contains(substring: \"balance\"),\n    Validator.contains(substring: \"deals\")\n])\nassert(orV.check(message: \"server balance update\"))\nassert(!orV.check(message: \"ping\"))\n\n// Use with raw handler\nlet handler = try await api.createRawHandler(\n    validator: Validator.contains(substring: \"\\\"balance\\\"\"),\n    keepAlive: nil\n)\nlet response = try await handler.sendAndWait(message: \"42[\\\"getBalance\\\"]\")\nprint(response)\n```\n",
    "csharp": "## C#\n\n```csharp\nusing UniFFI.BinaryOptionsToolsUni;\n\n// Accept everything\nvar open = Validator.New();\nassert(open.Check(\"any message\"));\n\n// Regex match\nvar v = Validator.Regex(@\"^\\d+\\[\\\"\");\nassert(v.Check(\"42[\\\"getBalance\\\"]\"));\n\n// Prefix / suffix / substring\nvar sw = Validator.StartsWith(\"42\");\nvar ew = Validator.EndsWith(\"}\");\nvar ct = Validator.Contains(\"\\\"balance\\\"\");\n\n// Negate\nvar notError = Validator.Ne(Validator.Contains(\"error\"));\nassert(notError.Check(\"success\"));\nassert(!notError.Check(\"error occurred\"));\n\n// AND composition\nvar andV = Validator.All(new List<Validator> {\n    Validator.StartsWith(\"42\"),\n    Validator.Contains(\"balance\")\n});\nassert(andV.Check(\"42[\\\"balance\\\",{}]\"));\n\n// OR composition\nvar orV = Validator.Any(new List<Validator> {\n    Validator.Contains(\"balance\"),\n    Validator.Contains(\"deals\")\n});\nassert(orV.Check(\"server balance update\"));\nassert(!orV.Check(\"ping\"));\n\n// Use with raw handler\nvar handler = await api.CreateRawHandler(Validator.Contains(\"\\\"balance\\\"\"), null);\nvar response = await handler.SendAndWait(\"42[\\\"getBalance\\\"]\");\nConsole.WriteLine(response);\n```\n",
    "go": "## Go\n\n```go\nimport bot \"github.com/ChipaDevTeam/BinaryOptionsTools-v2\"\n\n// Accept everything\nopen := bot.ValidatorNew()\nif !open.Check(\"any message\") { panic(\"expected true\") }\n\n// Regex match\nv, err := bot.ValidatorRegex(`^\\d+\\[\"`)\nif err != nil { panic(err) }\nif !v.Check(`42[\"getBalance\"]`) { panic(\"expected true\") }\n\n// Prefix / suffix / substring\nsw := bot.ValidatorStartsWith(\"42\")\new := bot.ValidatorEndsWith(\"}\")\nct := bot.ValidatorContains(`\"balance\"`)\n_ = sw; _ = ew; _ = ct\n\n// Negate\nnotError := bot.ValidatorNe(bot.ValidatorContains(\"error\"))\nif !notError.Check(\"success\") { panic(\"expected true\") }\nif notError.Check(\"error occurred\") { panic(\"expected false\") }\n\n// AND composition\nandV := bot.ValidatorAll([]bot.Validator{\n    bot.ValidatorStartsWith(\"42\"),\n    bot.ValidatorContains(\"balance\"),\n})\nif !andV.Check(`42[\"balance\",{}]`) { panic(\"expected true\") }\n\n// OR composition\norV := bot.ValidatorAny([]bot.Validator{\n    bot.ValidatorContains(\"balance\"),\n    bot.ValidatorContains(\"deals\"),\n})\nif !orV.Check(\"server balance update\") { panic(\"expected true\") }\nif orV.Check(\"ping\") { panic(\"expected false\") }\n\n// Use with raw handler\nhandler, err := api.CreateRawHandler(bot.ValidatorContains(`\"balance\"`), nil)\nif err != nil { panic(err) }\nresponse, err := handler.SendAndWait(`42[\"getBalance\"]`)\nfmt.Println(response)\n```\n",
    "ruby": "## Ruby\n\n```ruby\nrequire 'binary_options_tools_uni'\n\nV = BinaryOptionsToolsUni::Validator\n\n# Accept everything\nopen_v = V.new\nraise unless open_v.check('any message')\n\n# Regex match\nv = V.regex('^\\d+\\[\"')\nraise unless v.check('42[\"getBalance\"]')\n\n# Prefix / suffix / substring\nsw = V.starts_with('42')\new = V.ends_with('}')\nct = V.contains('\"balance\"')\n\n# Negate\nnot_error = V.ne(V.contains('error'))\nraise unless not_error.check('success')\nraise if not_error.check('error occurred')\n\n# AND composition\nand_v = V.all([\n    V.starts_with('42'),\n    V.contains('balance')\n])\nraise unless and_v.check('42[\"balance\",{}]')\n\n# OR composition\nor_v = V.any([\n    V.contains('balance'),\n    V.contains('deals')\n])\nraise unless or_v.check('server balance update')\nraise if or_v.check('ping')\n\n# Use with raw handler\nhandler = api.create_raw_handler(V.contains('\"balance\"'), nil)\nresponse = handler.send_and_wait('42[\"getBalance\"]')\nputs response\n```\n",
    "javascript": "## JavaScript\n\n```javascript\nimport { Validator } from 'binary-options-tools-uni';\n\n// Accept everything\nconst open = Validator.new();\nconsole.assert(open.check('any message'));\n\n// Regex match\nconst v = Validator.regex('^\\\\d+\\\\[\"');\nconsole.assert(v.check('42[\"getBalance\"]'));\n\n// Prefix / suffix / substring\nconst sw = Validator.startsWith('42');\nconst ew = Validator.endsWith('}');\nconst ct = Validator.contains('\"balance\"');\n\n// Negate\nconst notError = Validator.ne(Validator.contains('error'));\nconsole.assert(notError.check('success') === true);\nconsole.assert(notError.check('error occurred') === false);\n\n// AND composition\nconst andV = Validator.all([\n    Validator.startsWith('42'),\n    Validator.contains('balance'),\n]);\nconsole.assert(andV.check('42[\"balance\",{}]') === true);\nconsole.assert(andV.check('42[\"deals\",{}]') === false);\n\n// OR composition\nconst orV = Validator.any([\n    Validator.contains('balance'),\n    Validator.contains('deals'),\n]);\nconsole.assert(orV.check('server balance update') === true);\nconsole.assert(orV.check('ping') === false);\n\n// Use with raw handler\nconst handler = await api.createRawHandler(Validator.contains('\"balance\"'), null);\nconst response = await handler.sendAndWait('42[\"getBalance\"]');\nconsole.log(response);\n```\n"
  }
}
